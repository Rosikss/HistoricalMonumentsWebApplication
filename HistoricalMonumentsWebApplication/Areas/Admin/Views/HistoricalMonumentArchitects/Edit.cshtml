@model HistoricalMonumentArchitect

@{
    ViewData["Title"] = "Edit";
}

<h1>Редагувати</h1>

<h4>Архітектор історичної пам'ятки</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ArchitectId" class="control-label"></label>
                <select asp-for="ArchitectId" class="form-control" asp-items="ViewBag.ArchitectId"></select>
                <span asp-validation-for="ArchitectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HistoricalMonumentId" class="control-label"></label>
                <select asp-for="HistoricalMonumentId" class="form-control" asp-items="ViewBag.HistoricalMonumentId"></select>
                <span asp-validation-for="HistoricalMonumentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
